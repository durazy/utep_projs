name: omok_proj
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^1.2.0 # Ensure this version is compatible with your SDK version

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
